# She Nicest 前端开发文档（MVP）

| 项 | 内容 |
| --- | --- |
| 文档版本 | v1.0 |
| 日期 | 2026-08-29 |
| 前端形态 | 移动端优先 Web App，可选 PWA |
| 部署 | Vercel |
| 后端 | Supabase Auth / Data API / Edge Functions / Storage |
| 目标读者 | 已熟悉项目 DESIGN 规范，但对产品、业务和技术背景完全不了解的前端开发者 |

> 本文是前端实现的主文档。视觉颜色、字号、圆角、阴影、图标和基础组件外观以既有 DESIGN 规范为准；本文定义功能范围、路由、业务行为、组件状态、数据结构、接口契约、Mock 边界、异常处理和验收标准。

## 1. 产品一句话与前端目标

She Nicest 是面向更年期女性的健康陪伴与记录产品。MVP 最重要的闭环是：

```text
文字/语音表达近况
  → AI 温和回复
  → 自动提取健康记录草案
  → 用户逐条检查、编辑、勾选
  → 确认后保存正式健康记录
  → 月历/曲线、月度总结和就医报告使用这些记录
```

产品只整理事实、提供一般性陪伴和信息支持，不诊断、不处方、不替代医生。所有健康数据在用户确认前都只是草案。

前端完成标准：开发者仅使用本文和已有 DESIGN 规范，即可搭建全部 MVP 页面、真实接口适配层、Mock 模式、状态与错误处理、测试和 Vercel 发布，不需要再从 PRD 猜测交互。

## 2. 决策优先级

发生冲突时按以下优先级处理：

1. 本文标记为“冻结”的前端契约。
2. 《前后端接口设计.md》的冻结接口。
3. 《MVP技术方案.md》的范围和安全约束。
4. 《产品需求文档.md》的 MVP 范围。
5. 《健康信息提取原则.md》《健康卡片.md》的字段含义。
6. DESIGN 规范只决定视觉呈现，不改变业务行为。

不得自行扩展好友、社区、商城、成就、检查报告上传或药物管理后端。

## 3. MVP 范围

### 3.1 必须真实实现

| 模块 | MVP 功能 |
| --- | --- |
| 账号与角色 | Supabase 登录/Session；`self_user` 与 `supporter` 功能隔离 |
| 絮絮叨叨 | 多轮文字聊天、单句录音转写、SSE 流式回答、TTS、RAG 来源、安全提示、健康卡片草案确认、页面导航 |
| 健康卡片 | AI 草案预览；逐条勾选、编辑、确认；手动填写；当日与历史记录编辑 |
| 今天动一动 | 今日 2～3 条建议；回退链；六分类列表页 |
| 这个月的我 | 睡眠/潮热/心情/运动四维度；日历/曲线；单日卡片；月度小结 |
| 月度总结 | 当前月总结、历史列表、红点、分享；必须含“这个月发生的好事情” |
| 就医报告 | 时间窗口、数据不足提示、11 字段编辑、自动预览、前端 PDF、下载/分享 |
| AI 帮我说 | 文字/语音输入、AI 温和改写、预览编辑、复制、系统分享 |
| 我的资料 | 出生年份、既往病史、手术史读取与保存 |
| 导航 | 四个底部 Tab；默认进入“今天的我”；全局未实现入口统一 toast |

### 3.2 只展示，不建接口

下列数据必须来自前端静态 fixture，不访问 Supabase，不产生埋点或存储：

| 模块 | 展示内容 | 点击行为 |
| --- | --- | --- |
| 交流广场 | 搜索栏、帖子卡片、头像、昵称、图片、点赞/评论/收藏数、商城和发帖入口 | 全部 toast“敬请期待” |
| 我的消息其他内容 | 交友 icon、好友/消息提醒假列表 | 全部 toast“敬请期待” |
| 我的成就 | 12 枚全部锁定的成就 | 只展示，无解锁逻辑 |
| 我的收藏 | 入口 | toast“敬请期待” |
| 检查报告上传 | 入口 | toast“敬请期待” |
| 药物管理 | 入口 | toast“敬请期待” |
| 设置与隐私 | 入口 | toast“敬请期待” |

12 枚成就固定为：累计记录 100/365/500/1000 天、连续记录 100/365/500/1000 天、累计学习 100/500/1000/2000 篇。

### 3.3 明确不做

- 原生 iOS/Android 客户端和应用商店发布。
- 实时 ASR 字幕、持续监听、唤醒词和语音打断。
- 好友申请、好友关系、私聊、真实消息和通知。
- 帖子详情、搜索结果、发帖、点赞、评论、收藏和商城。
- 行动建议详情、分类详情和完成打卡。
- 成就触发、解锁、埋点和持久化。
- AI 直接写正式健康记录。
- 服务端 PDF 导出及最终 PDF 保存。
- 微信 SDK 分享；MVP 为 Web Share API + 下载/复制回退。

## 4. 用户角色与可见范围

```ts
export type UserType = "self_user" | "supporter";
```

| 能力 | `self_user` | `supporter` |
| --- | --- | --- |
| 登录、AI 聊天、ASR、TTS、RAG 问答 | 可用 | 可用 |
| 健康卡片和历史记录 | 可用 | 不显示；直接访问跳转首页并提示无权限 |
| 今天动一动 | 可用 | 可保留公共默认建议，不读取健康数据 |
| 月历、曲线、月度总结 | 可用 | 不显示/不可访问 |
| 就医报告 | 可用 | 不显示/不可访问 |
| 交流广场、消息、成就等展示页 | 可见 | 可见 |

前端显隐仅用于体验，不能视为安全边界；后端仍会返回 `403 ROLE_NOT_ALLOWED`。收到该错误时清除当前受限页面数据，toast“当前账号暂不支持此功能”，跳转 `/today`。

MVP 默认注册角色由部署配置或后端确定；如上线注册页确实提供选择，只允许首次选择，不提供设置页切换角色。

## 5. 技术栈与工程约定

### 5.1 推荐依赖

| 领域 | 选型 |
| --- | --- |
| 框架 | Next.js App Router + React + TypeScript strict |
| 部署 | Vercel |
| 后端 SDK | `@supabase/supabase-js`；若采用 SSR Cookie Session，再加 `@supabase/ssr` |
| 服务端状态 | `@tanstack/react-query` |
| 表单 | `react-hook-form` + `zod` |
| SSE | `fetch` + `ReadableStream`；可使用 `eventsource-parser`，不可使用原生 `EventSource` |
| 日期 | `date-fns`，显示与业务日期统一 `Asia/Shanghai` |
| 图表 | DESIGN 规范允许的图表库；无指定时使用 `recharts`，封装业务图表组件 |
| PDF | `@react-pdf/renderer`，嵌入有授权的中文字体 |
| 测试 | Vitest + Testing Library + Playwright |

不要同时引入多个全局状态库。React Query 管理服务器数据；聊天流、录音和弹窗使用页面内 reducer/context 即可。

### 5.2 建议目录

```text
src/
  app/
    (auth)/login/page.tsx
    (main)/layout.tsx
    (main)/today/page.tsx
    (main)/square/page.tsx
    (main)/messages/page.tsx
    (main)/me/page.tsx
    chat/page.tsx
    actions/page.tsx
    records/page.tsx
    records/edit/page.tsx
    summaries/page.tsx
    summaries/[month]/page.tsx
    report/page.tsx
    report/preview/page.tsx
    messages/compose/page.tsx
    me/profile/page.tsx
    me/achievements/page.tsx
  components/
    app-shell/
    chat/
    health-card/
    records/
    report/
    audio/
    feedback/
  features/
    auth/
    chat/
    health-records/
    recommendations/
    summaries/
    reports/
    profile/
  lib/
    api/
      contracts.ts
      edge-functions.ts
      health-rpc.ts
      errors.ts
    sse/
    supabase/
    date/
    pdf/
    share/
  mocks/
    fixtures/
    handlers/
  public/
    data/action-categories.json
    data/fake-feed.json
    data/fake-messages.json
    data/achievements.json
```

页面不得直接拼 Supabase URL 或数据库字段。所有请求通过 `features/*/service.ts`；数据库 `snake_case` 在服务层转换为 UI 使用的 `camelCase`。

### 5.3 环境变量

```bash
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
NEXT_PUBLIC_APP_ENV=development
NEXT_PUBLIC_USE_MOCKS=true
```

前端构建产物中只能存在 Project URL 和 Publishable Key。百炼 API Key、Supabase Secret/Service Role Key、数据库连接串不得放入 `NEXT_PUBLIC_*`，不得提交仓库。

开发、Vercel Preview、Vercel Production 使用不同环境值；Preview 默认连接测试 Supabase，禁止连接生产健康数据。

## 6. 路由、页面层级与 Tab

### 6.1 路由表

| 路由 | 页面 | 底部 Tab | 角色 | 数据 |
| --- | --- | --- | --- | --- |
| `/login` | 登录 | 无 | 全部 | Supabase Auth |
| `/today` | 今天的我 | 有，Tab 1 | 全部；健康模块按角色显隐 | 真实 + 静态 |
| `/chat` | 絮絮叨叨全屏聊天 | 无 | 全部 | 真实 |
| `/actions` | 六分类列表 | 无或返回栏 | 全部 | 静态分类；首页推荐真实 |
| `/records` | 这个月的我：四维度日历/曲线 | 无 | self_user | 真实 |
| `/records/edit?date=YYYY-MM-DD` | 健康卡片编辑 | 无 | self_user | 真实 |
| `/summaries` | 历史月度总结 | 无 | self_user | 真实 |
| `/summaries/[month]` | 月度总结详情 | 无 | self_user | 真实 |
| `/report` | 选择报告时间窗口 | 无 | self_user | 真实 |
| `/report/preview` | 报告预览编辑与 PDF | 无 | self_user | 真实；临时状态 |
| `/square` | 交流广场 | 有，Tab 2 | 全部 | 假数据 |
| `/messages` | 我的消息 | 有，Tab 3 | 全部 | 假数据 + AI 帮我说入口 |
| `/messages/compose` | AI 帮我说 | 无 | 全部 | 真实 |
| `/me` | 我的 | 有，Tab 4 | 全部 | 真实三字段 + 假入口 |
| `/me/profile` | 我的资料 | 无 | self_user | 真实三字段 |
| `/me/achievements` | 成就墙 | 无 | 全部 | 静态 |

登录后根路径 `/` 重定向 `/today`。全屏子页面使用顶部返回，不显示底部 Tab，返回优先 `router.back()`；无历史时回到所属 Tab。

### 6.2 Tab 状态

Tab 固定顺序：今天的我、交流广场、我的消息、我的。切换 Tab 保留各 Tab 的滚动位置；刷新后根据 URL 恢复当前 Tab。新月度总结红点显示在“今天的我”相关入口，不给假消息列表制造真实红点状态。

### 6.3 业务组件清单

基础 Button/Input/Card/Modal/Toast 等直接使用既有 DESIGN 组件；新增业务组件至少包括：

| 领域 | 组件 |
| --- | --- |
| Shell | `AppShell`、`BottomTabBar`、`PageHeader`、`RoleGuard`、`AsyncState` |
| 首页 | `ChatEntryCard`、`TodayActionsCard`、`ThisMonthCard`、`FloatingVoiceButton` |
| Chat | `MessageList`、`MessageBubble`、`ChatComposer`、`ToolStatus`、`RagSources`、`SafetyAlert`、`MessageAudioButton` |
| 语音 | `RecorderButton`、`RecordingOverlay`、`AudioPlayerController` |
| 健康卡片 | `HealthDraftModal`、`DraftItemRow`、`DraftDiff`、`HealthCategoryForm`、10 个分组字段组件 |
| 月度记录 | `DimensionTabs`、`ViewModeSwitch`、`HealthCalendar`、`MonthlyChart`、`DayRecordSheet`、`MonthlyDigest` |
| 总结 | `MonthlySummaryCard`、`GoodThingsSection`、`SummaryShareButton` |
| 报告 | `RangeSelector`、`DataWarning`、`ReportProfileForm`、`MedicationEditor`、`ReportPreview`、`PdfActions` |
| AI 帮我说 | `RephraseInput`、`RephrasePreview`、`CopyShareActions` |
| 静态展示 | `FakeFeedCard`、`FakeMessageRow`、`AchievementGrid` |

业务组件只接收 camelCase view model 和回调，不在内部调用 Supabase。页面/feature hook 负责请求和状态组合。

## 7. 全局交互规范

### 7.1 页面通用状态

每个真实数据区域必须具备：

- `loading`：使用 DESIGN 规范 Skeleton，不用无限空白。
- `empty`：说明为何为空，并给出下一步，例如“还没有记录，和 AI 聊聊或手动记录”。
- `error`：用户可理解的文案 + 重试；禁止直接展示 Supabase/模型原始错误。
- `ready`：正常内容。
- `forbidden`：按角色规则跳转并 toast。
- `offline`：保留已输入内容，提示网络恢复后重试，不虚构成功。

### 7.2 统一 toast

```ts
showComingSoon(); // 固定展示“敬请期待”
```

仅展示功能必须全部调用同一个方法。禁止不同页面出现“功能开发中”“暂未开放”等多个版本。

### 7.3 表单与离开保护

- 有未保存修改时，返回/关闭显示确认弹窗。
- 提交时按钮进入 loading 并禁用重复提交。
- 写请求生成 `Idempotency-Key`；网络重试复用同一个 key。
- 服务端校验错误定位到字段；未知错误显示页面级提示并保留输入。
- 日期使用 `YYYY-MM-DD` 传输，UI 使用中文本地格式。

### 7.4 移动端与无障碍

- 所有核心操作适配单手触达和软键盘；聊天输入区随键盘上移。
- 触控目标遵循 DESIGN 规范且不小于 44×44 CSS px。
- 流式文本区域使用适度的 `aria-live="polite"`，不得每个 token 都触发读屏；可按句更新可访问文本。
- 弹窗打开后锁定背景滚动和焦点；关闭后焦点返回触发按钮。
- 尊重 `prefers-reduced-motion`。
- 所有图表必须有文字摘要，不能只靠颜色表达好坏或正负。

## 8. 数据类型（前端冻结）

下列类型集中维护于 `lib/api/contracts.ts`，不得在组件内复制变体。

```ts
export type ISODate = `${number}-${number}-${number}`;
export type UserType = "self_user" | "supporter";
export type Severity = "轻" | "中" | "重";
export type Trend = "加重" | "减轻" | "稳定";

export interface Profile {
  id: string;
  userType: UserType;
  birthYear: number | null;
  medicalHistory: string;
  surgeryHistory: string;
}

export interface SymptomRecord {
  id?: string;
  symptom: string;
  occurred: boolean;
  severity?: Severity;
  frequency?: string;
  frequencyCount?: number;
  trend?: Trend;
  trigger?: string;
  quote: string;
}

export interface MoodRecord {
  id?: string;
  type: "负面" | "正面";
  description: string;
  trigger?: string;
}

export interface SleepRecord {
  id?: string;
  quality?: "好" | "一般" | "差";
  bedtime?: string;
  wakeTime?: string;
  nightWakes?: number;
  detail?: string;
}

export interface MenstrualRecord {
  id?: string;
  event: "来了" | "没来" | "量多" | "量少" | "淋漓不尽" | "非经期出血" | "痛经" | "停经";
  date?: ISODate;
  daysSinceLast?: number;
  note?: string;
}

export interface WeightRecord {
  id?: string;
  direction: "增加" | "减少";
  amount?: string;
  speed?: "突然" | "缓慢";
  date: ISODate;
}

export interface ExerciseRecord {
  id?: string;
  type: string;
  duration?: string;
  frequency?: string;
  intensity?: "轻松" | "适中" | "累";
}

export interface DietRecord {
  id?: string;
  mealsRegular?: boolean;
  foods?: string[];
  water?: "充足" | "偏少";
  caffeine?: string;
  alcohol?: string;
  smoking?: boolean;
}

export interface MedicationMention {
  id?: string;
  name: string;
  action: "服用" | "漏服" | "停用";
}

export interface LifeEventRecord {
  id?: string;
  category: "家庭" | "社交" | "心情事件";
  description: string;
  impact: "正面" | "负面" | "中性";
}

export interface HealthRecord {
  id?: string;
  date: ISODate;
  symptoms: SymptomRecord[];
  mood?: MoodRecord;
  sleep?: SleepRecord;
  menstrual?: MenstrualRecord;
  weight?: WeightRecord;
  appetite?: "增加" | "减少" | "正常";
  exercise?: ExerciseRecord;
  diet?: DietRecord;
  medications?: MedicationMention[];
  lifeEvents?: LifeEventRecord[];
  medicalNeeds?: string;
}
```

健康卡片在产品上显示 10 个分组：身体症状、情绪、睡眠与作息、月经与出血、体重与食欲、运动与活动、饮食与饮品、用药提及、生活事件与社交、就医诉求。数据协议为方便校验可拆为 11 个 `category`：

```ts
export type HealthCategory =
  | "symptom" | "mood" | "sleep" | "menstrual"
  | "weight" | "appetite" | "exercise" | "diet"
  | "medication" | "lifeEvent" | "medicalNeed";

export type DraftOperation = "create" | "update" | "delete";

export interface HealthDraftItem {
  clientItemId: string;
  category: HealthCategory;
  operation: DraftOperation;
  targetRecordId?: string;
  data?: Record<string, unknown>;
  before?: Record<string, unknown>;
  after?: Record<string, unknown>;
  quote: string;
  confidence: number;
}

export interface HealthCardDraft {
  draftId: string;
  sourceMessageId: string;
  recordDate: ISODate;
  items: HealthDraftItem[];
}
```

`confidence` 只用于前端辅助排序/提示，不保存为正式健康数据，不向用户展示伪精确百分比；低置信项可显示“请重点确认”。`quote` 必须在 AI 草案中回显。手动创建的条目由适配层补 `quote: "用户手动填写"`，UI 不显示这句。

## 9. API 基础约定

### 9.1 三类调用

| 类型 | 调用方式 | 用途 |
| --- | --- | --- |
| Auth | `supabase.auth.*` | 登录、Session、登出 |
| Data API/RPC | `supabase.from(...)` / `supabase.rpc(...)` | 资料、会话历史、记录读取、月度数据、总结列表 |
| Edge Function | 直接 `fetch("${SUPABASE_URL}/functions/v1/...")` | SSE Chat、ASR、TTS、确认、报告、AI 改写、推荐、总结生成 |

Vercel 不代理上述请求。所有登录后调用使用当前 Session：

```http
Authorization: Bearer <access_token>
```

Edge Function 成功直接返回业务 JSON，不额外包 `data`；失败格式统一为：

```ts
export interface ApiErrorBody {
  code: string;
  message: string;
  details?: Record<string, unknown>;
}
```

### 9.2 错误映射

| HTTP/code | 前端行为 |
| --- | --- |
| `401 AUTH_REQUIRED` | 尝试刷新 Session 一次；失败跳 `/login`，保留非敏感未发送文本于内存 |
| `403 ROLE_NOT_ALLOWED` | 清页面数据，toast 后跳 `/today` |
| `400 VALIDATION_ERROR` | 映射字段错误；无法映射则页面提示 |
| `404 DRAFT_NOT_FOUND` | 关闭草案弹窗，提示草案已失效，可重新发送/手动记录 |
| `409 ITEM_ALREADY_SAVED` | 视作可能成功，重新读取当日记录并提示已保存 |
| `413 ASR_TOO_LONG` | 提示录音超过 60 秒，请缩短后重录 |
| `415 ASR_UNSUPPORTED_FORMAT` | 提示当前浏览器录音格式暂不支持，改用文字 |
| `504/AI_TIMEOUT` | 保留用户消息，显示“重新生成” |
| `500/INTERNAL_ERROR/ASR_FAILED` | 通用错误，不清空输入，允许重试 |

不得把原始错误堆栈、SQL、模型返回、密钥或用户健康正文写入浏览器日志和第三方监控。

### 9.3 请求身份与幂等

- `clientMessageId`、`clientRequestId`、`Idempotency-Key` 使用 `crypto.randomUUID()`。
- 网络层自动重试 GET/RPC 读取最多一次；写请求只有明确幂等键才可重试。
- Chat 网络断开后的“继续重试”复用原 `clientMessageId`；用户主动“重新生成”使用新 ID。

## 10. 登录与启动流程

1. App 启动获取 Supabase Session。
2. 无 Session → `/login`。
3. 有 Session → 读取 `profiles`。
4. Profile 不存在 → 调用初始化流程创建默认/选择角色。
5. Profile 完整 → `/today`。

MVP 登录方式按后端实际配置展示，不自行增加微信登录。开发演示可使用 Supabase 测试手机号和固定验证码。

登录页状态：手机号输入、验证码输入、发送验证码倒计时、提交中、验证码错误、网络错误。验证码不可写死在前端源码。

登出时：停止聊天流和音频、清除 React Query 缓存、释放 Object URL、调用 `supabase.auth.signOut()`，再跳登录页。

## 11. 首页“今天的我”

### 11.1 页面结构

自上而下：

1. 顶部问候和日期。
2. “絮絮叨叨”主入口卡片，点击 `/chat`。
3. “今天动一动”卡片：2～3 条建议与“查看全部”。
4. “这个月的我”卡片：月度记录、月度总结、报告导出入口。
5. 全局悬浮语音按钮：点击进入 `/chat?mode=voice`，不在首页直接开始录音，避免权限和上下文混乱。

`supporter` 不显示“这个月的我”、健康卡片入口和报告入口；“今天动一动”只显示默认公共建议。

### 11.2 今日建议契约

```http
GET /functions/v1/recommendations
Authorization: Bearer <token>
```

```ts
export interface TodayRecommendationsResponse {
  source: "today" | "recent_3_days" | "default";
  items: Array<{
    id: string;
    category: "走起来" | "长肌肉" | "吃得好" | "骨头好" | "睡得好" | "心情好";
    title: string;
    description: string;
  }>;
}
```

前端最多展示 3 条，少于 2 条时用本地默认建议补足至 2 条。点击具体建议 toast；“查看全部”进入 `/actions`。

`/actions` 使用静态 JSON 展示六分类。分类卡片可以响应按压，但点击只 toast，不进入详情页。

### 11.3 首页状态

- 推荐加载失败：展示 2 条本地默认建议，不显示阻断错误。
- 月度数据无记录：入口仍可见，显示鼓励记录的空态。
- 月度总结有 `hasNew=true`：入口显示红点；查看详情后更新已读状态。

## 12. AI 聊天“絮絮叨叨”

### 12.1 页面结构

- 顶部：返回、标题、健康卡片入口。
- 固定免责声明：如“内容仅用于健康记录与一般信息支持，不能替代医生诊断”。必须在首次进入或页面可见区域展示，不依赖 AI 返回。
- 消息流：用户消息、AI 消息、工具状态、来源、安全提示、错误重试。
- 底部输入：多行文本、发送、麦克风；生成中显示停止按钮。
- 健康草案弹窗：允许在 `message_completed` 前或后出现。

消息角色：

```ts
export type ChatMessageStatus = "sending" | "streaming" | "completed" | "failed" | "stopped";

export interface ChatMessage {
  id: string;
  clientMessageId?: string;
  role: "user" | "assistant";
  content: string;
  status: ChatMessageStatus;
  sources?: RagSource[];
  speakableText?: string;
  createdAt: string;
}

export interface RagSource {
  documentId?: string;
  title: string;
  sourceUrl?: string;
  publisher?: string;
  reviewedAt?: string;
}
```

### 12.2 Chat 请求

```http
POST /functions/v1/chat
Authorization: Bearer <token>
Content-Type: application/json
Accept: text/event-stream
```

```ts
export interface ChatRequest {
  conversationId: string | null;
  clientMessageId: string;
  text: string;
  voiceReply: boolean;
}
```

发送规则：去除首尾空白后不能为空；限制长度由后端配置，前端默认 2000 字并显示计数。一次只允许一个生成请求。发送后立即插入用户消息，创建 AbortController，开始解析 SSE。

### 12.3 SSE 冻结事件

```ts
export type ChatSseEvent =
  | { type: "message_started"; data: { conversationId: string; clientMessageId: string } }
  | { type: "text_delta"; data: { delta: string } }
  | { type: "tool_status"; data: { name: string; status: "running" | "done" } }
  | { type: "rag_sources"; data: { sources: RagSource[] } }
  | { type: "health_card_preview"; data: HealthCardDraft }
  | { type: "navigation"; data: { target: NavigationTarget; params?: Record<string, string> } }
  | { type: "safety_alert"; data: { level: "urgent"; message: string } }
  | { type: "message_completed"; data: { messageId: string; speakableText: string } }
  | { type: "error"; data: { code: string; message: string; retryable: boolean } };

export type NavigationTarget =
  | "monthlyRecords" | "monthlySummary" | "reportExport"
  | "exerciseToday" | "exerciseCategories" | "healthCard" | "profile";
```

事件处理：

| 事件 | 行为 |
| --- | --- |
| `message_started` | 保存新会话 ID；创建/绑定 AI 占位消息 |
| `text_delta` | 追加而非替换文本；按动画帧批量更新，避免每 token 重渲染整个列表 |
| `tool_status` | 在 AI 消息下显示短暂状态；`done` 后隐藏或标记完成 |
| `rag_sources` | 去重后挂到当前 AI 消息底部；空数组等同不展示 |
| `health_card_preview` | 放入草案队列并打开第一项；不得因为弹窗阻塞 SSE 读取 |
| `navigation` | 已实现目标跳转；若正在编辑/有草案，先完成流并由用户确认跳转 |
| `safety_alert` | 立即在消息流中插入不可被普通气泡覆盖的高优先级提示；不得自动消失 |
| `message_completed` | 标记完成，保存 `speakableText`，结束加载 |
| `error` | 保留已有增量文本，标记失败；`retryable` 时显示重试 |

映射：

```ts
const targetRoutes: Record<NavigationTarget, string> = {
  monthlyRecords: "/records",
  monthlySummary: "/summaries",
  reportExport: "/report",
  exerciseToday: "/today#actions",
  exerciseCategories: "/actions",
  healthCard: `/records/edit?date=${todayInShanghai}`,
  profile: "/me/profile",
};
```

如果目标因角色不可访问，执行角色拒绝流程；如果以后收到未知 target，只 toast“敬请期待”，禁止拼接任意 URL。

### 12.4 SSE 解析要求

- 按 SSE 规范以空行分隔事件，处理跨 chunk 的半行和多字节 UTF-8。
- 忽略 `: ping` 注释。
- 单个坏 JSON 事件记录脱敏错误并跳过；`error`/HTTP 错误按统一映射处理。
- 连接自然结束但没有 `message_completed`：标记失败并允许重试。
- 用户点停止：调用 `abort()`，保留已显示文本，状态设为 `stopped`，不显示系统错误。
- 离开页面、登出或开始新请求时取消旧流。

### 12.5 会话历史

前端服务契约：

```ts
interface ConversationRow {
  id: string;
  title: string | null;
  createdAt: string;
  updatedAt: string;
}

interface MessagePage {
  items: ChatMessage[];
  nextCursor: string | null;
}
```

通过 Data API 查询当前用户会话和消息，按 `created_at` 倒序分页，再在 UI 中正序渲染。首次打开可创建空会话；不把全部历史拼入前端 Chat 请求，后端根据 `conversationId` 管理上下文。

## 13. 录音、ASR 与 TTS

### 13.1 录音状态机

```ts
type RecorderState =
  | "idle" | "requesting_permission" | "recording"
  | "stopping" | "uploading" | "transcribed" | "error";
```

流程：

1. 点击麦克风，请求权限。
2. 权限允许后开始录音和计时；50 秒提示，60 秒自动停止。
3. 再次点击手动停止，生成 Blob。
4. 调用 ASR。
5. 成功文本填入输入框，用户可编辑，**不得自动发送**。
6. 释放 MediaStream track 和临时 Blob 引用。

开始录音前暂停当前 TTS。页面不可见、路由离开或权限撤销时停止录音。浏览器不支持 `MediaRecorder` 时隐藏/禁用麦克风并提示使用文字。

MIME 优先级通过 `MediaRecorder.isTypeSupported()` 实测选择：

```ts
const preferredMimeTypes = [
  "audio/webm;codecs=opus",
  "audio/mp4",
  "audio/webm",
];
```

不得硬编码所有浏览器都输出 webm。

### 13.2 ASR 接口

```http
POST /functions/v1/speech-asr
Authorization: Bearer <token>
Content-Type: multipart/form-data
```

FormData：`audio`、`clientRequestId`、`durationMs`、`locale=zh-CN`。

```ts
interface AsrResponse {
  requestId: string;
  text: string;
  durationMs: number;
}
```

成功但 `text.trim()` 为空时视为未识别，提示重录，不覆盖输入框原内容。

### 13.3 TTS

只对已完成且存在 `speakableText` 的 AI 消息显示播放按钮。

```http
POST /functions/v1/speech-tts
Authorization: Bearer <token>
Content-Type: application/json

{"messageId":"msg_456","text":"..."}
```

响应为 `audio/mpeg` Blob。创建 Object URL 交给单例播放器；同时只允许一条消息播放。重新播放从 0 开始。切换播放消息、录音、登出或卸载时暂停并 `URL.revokeObjectURL()`。

TTS 失败只影响播放按钮，文字消息保持正常；不得自动重复请求造成费用。

## 14. 健康卡片草案

### 14.1 弹窗结构

弹窗顶部展示记录日期和来源说明；列表按产品 10 个分组排列。每条包含：

- 默认勾选框。
- 类别和摘要。
- AI 原文引用 `quote`。
- 新增/修改/删除标签。
- 编辑按钮。
- 修改时显示 `before → after` 差异；删除显示被删内容。
- “保存所选”和“稍后处理/关闭”。

用户取消勾选表示本次不保存，不表示删除草案中的原始信息。无选中项时保存按钮禁用。

多个草案连续到达时放入队列，一次只开一个弹窗；完成/关闭后处理下一项。

### 14.2 编辑字段

| 分组 | 字段与控件 | 校验 |
| --- | --- | --- |
| 身体症状 | symptom 选择/输入；occurred 开关；severity 单选；frequency 文本；frequencyCount 数字；trend 单选；trigger 文本 | symptom 必填；次数为非负整数 |
| 情绪 | type 单选；description；trigger | type、description 必填 |
| 睡眠 | quality；bedtime、wakeTime 时间；nightWakes 数字；detail | 夜醒为非负整数 |
| 月经与出血 | event；date；daysSinceLast；note | event 必填；天数非负 |
| 体重 | direction；amount；speed；date | direction、date 必填 |
| 食欲 | 增加/减少/正常 | 必填 |
| 运动 | type；duration；frequency；intensity | type 必填 |
| 饮食与饮品 | mealsRegular；foods tags；water；caffeine；alcohol；smoking | 可全选填，但空条目不提交 |
| 用药提及 | name；action | 两者必填；这里只记录动态提及 |
| 生活事件 | category；description；impact | 三者必填 |
| 就医诉求 | 文本 | 去空格后非空 |

不在日常卡片内要求填写既往病史、手术史、过敏史等报告静态字段。

### 14.3 确认接口

```http
POST /functions/v1/health-card-confirm
Authorization: Bearer <token>
Idempotency-Key: <uuid>
Content-Type: application/json
```

```ts
interface ConfirmDraftRequest {
  draftId: string;
  selectedItems: Array<{
    clientItemId: string;
    operation: "create" | "update" | "delete";
    targetRecordId?: string;
    data?: Record<string, unknown>;
  }>;
}

interface ConfirmDraftResponse {
  recordId: string;
  recordDate: ISODate;
  savedItemCount: number;
}
```

`update` 使用编辑后的完整/变更数据；`delete` 不带 data。提交成功：关闭弹窗、toast“已保存到健康记录”、失效当日记录/月度统计/报告覆盖度 Query。失败保留用户编辑和勾选状态。

草案进入表单时统一归一化：`create` 的初始值取 `item.data`；`update` 的初始值取 `item.after ?? item.data`；`delete` 只读展示 `item.before`。提交时 create/update 都写入 `selectedItems[].data`，不要把 `before/after` 原样提交给 confirm。

## 15. 手动健康卡片与历史编辑

### 15.1 读取契约

```ts
// supabase.rpc("get_health_record", { target_date: date })
interface GetHealthRecordResponse {
  record: HealthRecord | null;
}
```

`null` 展示空表单；有数据按 10 分组显示。页面支持添加症状/用药/生活事件多条，其余按当前 Schema 为单项。

### 15.2 保存契约

```ts
// supabase.rpc("save_health_record", { target_date: date, payload })
interface SaveHealthRecordRequest {
  targetDate: ISODate;
  payload: HealthRecord;
}

interface SaveHealthRecordResponse {
  recordId: string;
  recordDate: ISODate;
  updatedAt: string;
}
```

保存前显示确认摘要；成功后使 `healthRecord(date)`、`monthlyStats(month)`、`recommendations(today)` 和报告覆盖度失效。删除某个子项由 `save_health_record` 的最终 payload 表达；删除整日记录不在 MVP 提供快捷操作，除非后端另行冻结契约。

页面至少支持：今天、从月历点入的历史日期。未来日期不可选。用户编辑历史记录时明显显示日期，避免误改今日。

## 16. 这个月的我：日历与曲线

### 16.1 页面结构

- 月份切换；默认当前月，不允许进入未来月份。
- 维度 Tab：我的睡眠、我的潮热、我的心情、我的运动。
- 视图切换：日历 / 月度曲线。
- “记录”入口：`/records/edit?date=当天`。
- 点击有/无数据日期均可进入对应日期的卡片查看/填写。
- 底部月度小结。
- 月度总结与报告入口。

运动维度只来自健康卡片 `exercise`，不来自打卡，因为 MVP 没有打卡。

### 16.2 月度聚合契约

```ts
// supabase.rpc("get_monthly_stats", { target_month: "2026-08" })
interface MonthlyStatsResponse {
  month: `${number}-${number}`;
  days: Array<{
    date: ISODate;
    hasRecord: boolean;
    sleep?: {
      quality: "好" | "一般" | "差" | null;
      qualityScore: 1 | 2 | 3 | null; // 差1、一般2、好3
      nightWakes: number | null;
    };
    hotFlash?: {
      occurred: boolean;
      frequencyCount: number | null;
      severity: Severity | null;
      chartValue: number | null; // frequencyCount 优先，否则轻1/中2/重3
    };
    mood?: {
      type: "正面" | "负面" | null;
      description: string | null;
      chartValue: 1 | -1 | null;
    };
    exercise?: {
      type: string;
      duration: string | null;
      intensity: "轻松" | "适中" | "累" | null;
    };
  }>;
  digest: {
    text: string;
    highlights: string[];
  } | null;
}
```

日历必须包含该月所有日期，包括无记录日。图表不在前端重新推断医学趋势，只显示后端聚合值。空值表示没有数据，不画成 0；`occurred:false` 可显示“当天未发生”，与无记录区分。

### 16.3 图表规则

- 睡眠：主曲线 `qualityScore`，可辅以夜醒次数；图例说明 1/2/3 含义。
- 潮热：`chartValue`；Tooltip 同时显示真实来源是次数还是程度回退。
- 心情：正面 1、负面 -1，只做记录展示，不映射医学量表。
- 运动：按日期显示类型/时长；无法数值化的时长不强行画连续曲线，可用事件点/柱状标记。
- 每个图表下方显示文字摘要和“仅基于已确认记录”。

## 17. 月度总结

### 17.1 生成与读取

前端服务契约：

```http
POST /functions/v1/monthly-summary
Authorization: Bearer <token>
Content-Type: application/json

{"month":"2026-08"}
```

```ts
interface MonthlySummary {
  id: string;
  month: `${number}-${number}`;
  overview: string;
  goodThings: string[]; // 必须渲染为“这个月发生的好事情”板块
  dimensions: Array<{
    key: "sleep" | "hotFlash" | "mood" | "exercise" | "menstrual" | "weight";
    title: string;
    content: string;
  }>;
  generatedAt: string;
  hasNew: boolean;
}
```

进入当月详情：先查 `monthly_summaries`；不存在则调用生成函数并显示明确的生成状态。为避免重复调用，用 month 作为幂等键。当前接口方案以“首次访问懒生成”为 MVP；若后端上线 Cron，前端无需改变。

历史列表按月份倒序。查看某月详情后更新 `has_new=false`；若后端尚未提供更新 RPC，服务层预留 `markMonthlySummaryRead(id)`，Mock 模式可完整演示。

### 17.2 分享

分享优先 `navigator.share({ title, text })`；不支持时提供“复制总结”。不能把敏感健康内容自动发送给任何联系人，必须由用户主动触发系统分享。

## 18. 就医报告

### 18.1 时间窗口页

选项固定：`1_month`、`3_months`、`6_months`。前端查询当前用户最早健康记录日期：

```ts
interface ReportCoverage {
  hasAnyRecord: boolean;
  earliestRecordDate: ISODate | null;
  latestRecordDate: ISODate | null;
  availableRanges: Array<"1_month" | "3_months" | "6_months">;
}
```

可用判断以最早记录是否覆盖窗口起点为准，不要求每天都有记录。默认选择最长可用窗口。覆盖不足的选项置灰并说明原因。

若完全无记录，用户点击继续时弹窗“当前没有健康记录，是否继续生成空模板？”；确认后跳预览页并以空 summary 开始，不调用会失败的聚合流程。

### 18.2 预览接口

```http
POST /functions/v1/report-preview
Authorization: Bearer <token>
Content-Type: application/json

{"range":"3_months","chiefComplaint":"希望了解潮热和睡眠问题"}
```

```ts
type ReportRange = "1_month" | "3_months" | "6_months";

interface ReportProfile {
  birthYear: number | null;
  height: number | null;
  menopausalStatus: "未绝经" | "围绝经" | "已绝经" | null;
  usesMedication: boolean | null;
  chiefComplaint: string;
  medicalHistory: string;
  surgeryHistory: string;
  medications: Array<{
    name: string;
    dose?: string;
    frequency?: string;
    startDate?: ISODate;
    status: "服用中" | "已停用" | "偶尔服用";
    reason?: string;
  }>;
  allergies: string[];
  pregnancyHistory: string | null;
  familyHistory: string | null;
  screenings: string | null;
}

interface ReportPreviewResponse {
  reportId: null;
  range: { startDate: ISODate; endDate: ISODate };
  profile: ReportProfile;
  summary: {
    menstrual: string;
    symptoms: Array<{
      symptom: string;
      days: number;
      frequencySummary: string;
      severityMode: Severity | null;
      trend: Trend | null;
      quotes: string[];
    }>;
    weight: string;
    exercise: string;
  };
  dataWarnings: string[];
}
```

### 18.3 11 个可编辑字段

1. 出生年份/年龄。
2. 身高。
3. 绝经状态。
4. 是否用药及药物清单。
5. 想解决的问题，非必填。
6. 既往病史。
7. 手术史。
8. 过敏史。
9. 孕产史。
10. 家族史。
11. 筛查史。

出生年份、既往病史、手术史优先带入“我的资料”。“是否用药”使用 `usesMedication` 三态表达：`true` 是正在使用、`false` 是明确未使用、`null` 是尚未填写；不能用空药物数组推断。其余只在本次报告预览中编辑；MVP 不要求保存成完整资料档案。`dataWarnings` 在预览顶部展示，但不阻止用户生成 PDF。

### 18.4 PDF

- A4 固定模板；内容来自当前预览状态，不再次调用后端。
- 嵌入合法可分发的中文字体，验证粗体、换行、分页和空字段占位。
- 文件名：`健康报告_YYYY-MM-DD_至_YYYY-MM-DD.pdf`。
- “下载”创建 Blob 并触发下载。
- “分享”仅在 `navigator.canShare({ files: [...] })` 为 true 时分享 PDF File；否则回退下载并提示用户从系统文件中分享。
- 生成后释放 Object URL；最终 PDF 不上传 Supabase。

## 19. “AI 帮我说”

页面用于把用户口述整理成温和表达，不读取或写入健康记录。

流程：文字输入或复用 ASR → 点击“帮我整理” → AI 输出 → 可编辑预览 → 复制/系统分享。

前端冻结契约：

```http
POST /functions/v1/ai-rephrase
Authorization: Bearer <token>
Content-Type: application/json
```

```ts
interface AiRephraseRequest {
  clientRequestId: string;
  text: string;
  style: "warm";
}

interface AiRephraseResponse {
  text: string;
}
```

输入默认最多 2000 字；输出为空视为失败。复制成功显示反馈。分享使用 `navigator.share({ text })`，不支持则保留复制。微信 SDK 不实现。

如果后端尚未部署此函数，Mock 模式必须提供成功、超时和空输出案例；真实模式显示“暂时无法整理”，不得在前端自行调用百炼。

## 20. “我的”与个人资料

`/me` 展示用户基础信息、成就、收藏、资料、设置入口。只有“我的资料”三个字段和登出是真实功能，其余按范围处理。

资料读写使用 `profiles`：

```ts
interface EditableProfileForm {
  birthYear: number | null;
  medicalHistory: string;
  surgeryHistory: string;
}
```

校验：出生年份为合理四位整数且不晚于当前年；病史、手术史允许空，显示“无/未填写”的区别由产品文案决定，不自行把空值改成“无”。保存成功后失效 Profile 与报告预览相关缓存。

报告页允许临时补填更多字段，但“我的资料”页不能因此扩成 11 字段资料管理页。

## 21. 交流广场、消息和成就的静态实现

### 21.1 Fixture 原则

- Fixture 单独放 `mocks/fixtures` 或 `public/data`，不得散落组件。
- ID 稳定，图片使用项目本地资产或明确允许的占位资源。
- 假数据不写入 localStorage/Supabase。
- 点赞按钮可展示按压态，但数值和持久状态不能变化，以免造成真实功能错觉；点击 toast。

### 21.2 广场

只实现一个信息流页面。搜索栏、帖子卡片、交互按钮、商城 icon、悬浮“+”都可见，但均不导航到新页面。

### 21.3 我的消息

顶部“AI 帮我说”进入真实 `/messages/compose`。交友 icon 和消息列表为假数据，点击 toast。

### 21.4 成就

入口进入 `/me/achievements`，展示 12 枚锁定态徽章。禁止读取记录天数、计算进度、显示动态百分比或发送埋点。

## 22. Mock 架构

前端必须能在后端未就绪时完成全流程演示。

```ts
export interface FrontendServices {
  chat: ChatService;
  speech: SpeechService;
  healthRecords: HealthRecordService;
  recommendations: RecommendationService;
  summaries: SummaryService;
  reports: ReportService;
  profile: ProfileService;
  rephrase: RephraseService;
}

export interface ChatService {
  stream(request: ChatRequest, signal: AbortSignal): AsyncIterable<ChatSseEvent>;
  getMessages(conversationId: string, cursor?: string): Promise<MessagePage>;
}

export interface SpeechService {
  transcribe(input: {
    audio: Blob;
    fileName: string;
    mimeType: string;
    clientRequestId: string;
    durationMs: number;
    locale: "zh-CN";
  }): Promise<AsrResponse>;
  synthesize(input: { messageId: string; text: string }): Promise<Blob>;
}

export interface HealthRecordService {
  getByDate(date: ISODate): Promise<HealthRecord | null>;
  save(record: HealthRecord, idempotencyKey: string): Promise<SaveHealthRecordResponse>;
  confirmDraft(request: ConfirmDraftRequest, idempotencyKey: string): Promise<ConfirmDraftResponse>;
  getMonthlyStats(month: string): Promise<MonthlyStatsResponse>;
  getReportCoverage(): Promise<ReportCoverage>;
}

export interface RecommendationService {
  getToday(): Promise<TodayRecommendationsResponse>;
}

export interface SummaryService {
  list(): Promise<MonthlySummary[]>;
  get(month: string): Promise<MonthlySummary | null>;
  generate(month: string): Promise<MonthlySummary>;
  markRead(id: string): Promise<void>;
}

export interface ReportService {
  preview(input: { range: ReportRange; chiefComplaint: string }): Promise<ReportPreviewResponse>;
}

export interface ProfileService {
  get(): Promise<Profile>;
  update(input: EditableProfileForm): Promise<Profile>;
}

export interface RephraseService {
  rephrase(input: AiRephraseRequest): Promise<AiRephraseResponse>;
}
```

`NEXT_PUBLIC_USE_MOCKS=true` 注入 Mock 实现，false 注入 Supabase 实现。页面只依赖接口，不判断 Mock 开关。

必须提供的 Mock 场景：

| 功能 | 场景 |
| --- | --- |
| Chat | 正常流；RAG 来源；健康草案；导航；安全提示；中途断流；可重试错误 |
| 草案 | create/update/delete；无选中；草案失效；幂等冲突 |
| ASR | 成功；空文本；超时；超长；不支持格式 |
| TTS | 成功音频；失败降级 |
| 月度数据 | 完整数据；零散数据；全空；明确 `occurred:false` |
| 报告 | 1/3/6 月覆盖；无数据空模板；dataWarnings；长中文分页 |
| 角色 | self_user；supporter；运行中被 403 拒绝 |
| 静态页 | 所有入口都触发同一个 toast |

Mock SSE 必须按真实事件格式和分片方式输出，包含拆开的 UTF-8/chunk 情况，不能一次返回完整字符串掩盖解析问题。

## 23. 缓存与 Query Key

建议统一：

```ts
const queryKeys = {
  profile: ["profile"] as const,
  conversation: (id: string) => ["conversation", id] as const,
  healthRecord: (date: ISODate) => ["health-record", date] as const,
  monthlyStats: (month: string) => ["monthly-stats", month] as const,
  monthlySummaries: ["monthly-summaries"] as const,
  monthlySummary: (month: string) => ["monthly-summary", month] as const,
  recommendations: (date: ISODate) => ["recommendations", date] as const,
  reportCoverage: ["report-coverage"] as const,
};
```

健康记录确认/保存后，至少失效：对应日期记录、对应月统计、今日推荐、报告覆盖度；月度总结若已生成不自动静默覆盖，显示需要重新生成或由后端版本策略处理。

页面卸载可保留当前 Session 内的 React Query 内存缓存，但不得把健康正文持久化到 localStorage、IndexedDB、CDN 或 Service Worker Cache。PWA 如启用，只缓存静态壳和公开静态 JSON，不缓存 Auth/健康/聊天响应。

## 24. 安全、隐私与内容渲染

- 百炼与 Service Role 密钥永不进入前端。
- 健康数据依赖 Supabase RLS；所有查询限定当前用户，不接受 URL 中任意 userId。
- AI Markdown 使用安全渲染器和 allowlist，禁用原始 HTML；外链加 `rel="noopener noreferrer"`。
- RAG 来源 URL 只允许 `http/https`，未知协议不渲染为链接。
- 麦克风权限只在用户点击时请求，权限文案说明用途；不后台录音。
- 录音 Blob 转写后释放，不上传 Vercel，不写浏览器持久存储。
- 安全提示、免责声明不能被普通消息覆盖或自动消失。
- Sentry/Analytics 不发送聊天文本、录音、健康字段、报告内容、access token 或 URL query 中的健康数据。
- 报告预览状态优先放内存/context；刷新丢失时返回时间窗口页重新生成，不把完整报告塞 URL/localStorage。

## 25. 性能要求

- 首屏只加载当前 Tab；聊天、图表、PDF 模块按路由动态加载。
- 流式消息更新按帧/小批次合并，避免每个 token 导致整个消息列表重排。
- 长消息列表虚拟化可在真实数据超过约 100 条后启用；MVP 至少分页加载历史。
- 图表只渲染当前维度和当前视图。
- PDF 库只在预览页动态导入。
- 图片使用 Next Image 或 DESIGN 规范提供的优化组件；静态假数据图不得引用不稳定远程热链。
- 在 4G 弱网下，发送、停止、重试和断流状态必须可理解。

## 26. 测试要求

### 26.1 单元测试

- snake_case/camelCase 适配。
- SSE 跨 chunk、心跳、坏 JSON、缺 completed、Abort。
- 错误码映射。
- 健康草案 create/update/delete 到 confirm payload。
- 日期、报告窗口可用性、图表空值与程度回退。
- share/download capability 检测。

### 26.2 组件测试

- Chat 增量文本、来源、安全提示、停止与重试。
- 健康草案勾选、编辑、差异、失败保留。
- MediaRecorder 权限拒绝、60 秒自动停止、ASR 回填不自动发送。
- 报告 11 字段表单和 dataWarnings。
- supporter 路由守卫。
- 静态入口全部调用 `showComingSoon()`。

### 26.3 E2E 主路径

1. 登录 → 默认今天的我。
2. 进入聊天 → 文字发送 → 流式回答 → 健康草案 → 编辑勾选 → 保存 → 月历看到记录。
3. 录音 → ASR 回填 → 用户编辑 → 发送 → 播放 TTS。
4. AI 修改历史记录 → before/after → 确认 → 数据刷新。
5. 四维度日历/曲线切换 → 点击单日 → 编辑保存。
6. 月度总结生成 → 好事情板块 → 分享/复制 → 红点消失。
7. 报告选择窗口 → 预览 → 编辑 11 字段 → PDF 下载；支持设备上分享。
8. AI 帮我说 → 语音/文字 → 改写 → 编辑 → 复制/分享。
9. supporter 直接访问健康 URL → 被拒绝并回首页。
10. 广场/消息/收藏/设置/成就交互 → 无网络请求，统一 toast。

### 26.4 浏览器测试

至少覆盖目标版本的：

- iOS Safari。
- Android Chrome。
- 微信内置浏览器（若产品演示会从微信打开）。
- 桌面 Chrome，仅用于开发和报告打印回归。

重点实测 MediaRecorder MIME、ReadableStream SSE、Blob PDF 下载、Web Share API、软键盘和安全区。能力不支持时必须有回退，不能只在桌面 Chrome 验收。

## 27. 埋点与日志边界

当前 PRD 明确成就无埋点；项目未冻结业务埋点方案前，不自行添加用户行为分析。允许的技术指标只记录匿名、非内容型数据，例如接口成功/失败、耗时、浏览器能力支持，不包含用户文本或健康值。

前端可为请求传递 `X-Request-Id`，错误监控只记录 request ID、错误码、路由、应用版本和浏览器类型。

## 28. Vercel 发布清单

- Production 与 Preview 环境变量分离。
- Production 构建关闭 Mock；Preview 可显式选择 Mock 或测试 Supabase。
- 设置安全响应头：CSP、`X-Content-Type-Options`、合理的 `Referrer-Policy`。
- CSP 允许当前 Supabase 项目的 HTTPS/WSS、所需字体/图片源，不使用 `*`。
- 禁止把健康内容放进页面 metadata、URL、日志和静态生成文件。
- 登录回调 URL、Site URL、Supabase CORS/Redirect URL 配置正式域名与受控 Preview 域名。
- 对 Vercel 前端、Supabase API、SSE、ASR、TTS 做中国大陆真实移动网络测试。
- 发布前检查前端 bundle 不含百炼 API Key、Service Role Key 和测试账号验证码。

## 29. 开发顺序

1. 工程、DESIGN 组件接入、路由、Tab、统一状态、Mock 注入。
2. 登录、Profile、角色守卫。
3. 首页与所有静态展示页，先锁定“不发请求”边界。
4. Chat SSE、消息状态、RAG 来源、安全提示、导航。
5. 健康草案弹窗、确认接口、手动卡片。
6. ASR/TTS 与真机浏览器兼容。
7. 月历/曲线、月度小结和今日建议。
8. 月度总结与红点。
9. 报告窗口、预览、PDF 和分享。
10. AI 帮我说。
11. E2E、弱网、角色、安全和 Vercel 发布验证。

## 30. 前端 Definition of Done

一个功能只有同时满足以下条件才算完成：

- 行为符合本文及 DESIGN 规范。
- loading/empty/error/ready/forbidden 状态齐全。
- 真实与 Mock 服务使用同一类型契约。
- 不越过角色权限和 MVP 假数据边界。
- 写操作幂等，失败不丢输入。
- 移动端主流目标浏览器验证通过。
- 至少有关键单测/组件测试，主闭环有 E2E。
- 无敏感信息日志、无前端秘密、无健康响应的持久/CDN 缓存。
- 未实现入口统一 toast，不出现死链或空白页。

## 31. 联调前必须由后端最终确认的契约

前端可以依靠 Mock 完成，但真实联调前以下内容必须冻结；字段变化只允许修改服务适配层：

1. `chat` 八类 SSE 事件、结束/断流语义。
2. 健康草案 `category/operation/before/after`。
3. `health-card-confirm` 幂等规则。
4. ASR 支持 MIME、体积限制和响应 Content-Type。
5. TTS 是直接音频二进制还是短期 URL；本文按二进制实现。
6. `get_health_record`、`save_health_record`、`get_monthly_stats` 三个 RPC 的 JSON。
7. `monthly-summary`、`recommendations`、`ai-rephrase` Edge Functions。
8. 月度总结 `hasNew` 的更新方式。
9. 报告覆盖度判定和 `report-preview` 空字段类型。
10. Profile 的 `user_type` 创建与 supporter 开通方式。

任何一项暂未完成时，前端使用本文 Mock 契约继续开发，不得绕开服务层把临时响应形状写进组件。

## 附录 A：建议静态 fixture

```ts
export const actionCategories = [
  { id: "walk", title: "走起来", description: "步行与日常活动" },
  { id: "strength", title: "长肌肉", description: "力量训练" },
  { id: "nutrition", title: "吃得好", description: "饮食营养" },
  { id: "bone", title: "骨头好", description: "骨骼健康" },
  { id: "sleep", title: "睡得好", description: "睡眠改善" },
  { id: "mood", title: "心情好", description: "情绪调节" },
] as const;

export const achievements = [
  ...[100, 365, 500, 1000].map((n) => ({ type: "total_records", target: n })),
  ...[100, 365, 500, 1000].map((n) => ({ type: "continuous_records", target: n })),
  ...[100, 500, 1000, 2000].map((n) => ({ type: "total_learning", target: n })),
].map((item, index) => ({ ...item, id: `achievement_${index + 1}`, locked: true }));
```

广场和消息 fixture 的具体文案与图片由产品/设计提供；未提供时可使用中性演示内容，但不得写成真实医学建议或真实用户隐私。

## 附录 B：资料来源

- 《产品需求文档.md》
- 《MVP技术方案.md》
- 《前后端接口设计.md》
- 《健康信息提取原则.md》
- 《健康卡片.md》
