# 当前实现更新说明

> 更新日期：2026-09-05。本文以当前代码为准，补充并覆盖早期文档中“对话提取后必须弹窗确认才写卡”的旧流程。

## 1. 絮絮叨叨 AI 对话

- 对话为全屏工作区：对话流独立滚动，文字/语音输入栏始终固定在界面底部。
- AI 回复按 Markdown 渲染，支持段落、标题、列表、引用、链接、代码与表格；服务端提示词要求短段落、非 HTML 输出。
- 页面始终展示“不能替代医生诊断；急症及时就医”的免责声明。后端会先识别危急表达并通过 SSE 发送安全提示。
- 语音输入使用 `MediaRecorder` 录音，调用 `speech-asr` 转写；转写成功后作为本轮对话文本发送。AI 回复可调用 `speech-tts` 播放，失败时不影响文字回复。
- 后端可按用户问题读取个人资料、已确认的月度记录，并把审核知识库检索结果作为 RAG 上下文；只在问题相关时注入资料，且不主动复述隐私字段。
- AI 可通过 `navigation` SSE 事件唤起健康卡片、月度记录、月度总结、报告、个人资料或行动建议，前端以功能卡片承接跳转。

## 2. 健康数据自动提取与写入

当前流程是“自动提取、自动更新、可追溯可编辑”，不再弹出逐条确认弹窗：

1. 后端保存用户消息和 AI 回复。
2. 根据用户原话及目标日期读取已有健康记录。
3. 提取结构化 `HealthDraftItem[]`，支持 `create`、`update`、`delete` 三种操作。
4. 校验类别、字段形状、枚举、时间格式、目标记录 ID 和置信度；模型结构不合格时允许一次 JSON 修复，再过滤无效项。
5. 用 `applyDraftItems` 合并到当日或文本指向日期的 `HealthRecord`，经 `save_health_record` RPC 自动持久化。
6. 成功后服务端发送 `health_card_updated`；前端在健康卡片编辑图标旁显示“健康卡片已更新：……”提示气泡。失败时显示失败提示，并保留手动编辑入口。

提取范围包括：症状、睡眠、情绪、月经与出血、体重、食欲、运动、饮食饮品、用药、生活事件、就医诉求、其他记录。系统只记录用户明确陈述的事实，不生成诊断、处方或自行调整用药建议。

> 自动写入是当前已实现行为；用户可随时在健康卡片中修改、补充或删除。旧文档中“必须弹窗确认后才能写库”的表述已不再适用。

## 3. 提取模式与 Mock 模式

| 运行模式 | 触发条件 | 提取方式 |
| --- | --- | --- |
| 真实后端模式 | `NEXT_PUBLIC_USE_MOCKS=false` | Edge Function 调用结构化输出模型，传入用户原话和已有记录，进行 JSON 校验/修复后自动写卡。 |
| Mock 模式 | `NEXT_PUBLIC_USE_MOCKS=true` 或后端 Mock 配置 | 关键词与正则规则：识别常见症状、否定、次数、程度、趋势、睡眠、情绪、运动、用药、就医诉求及修改/删除意图。 |

当前本地 `frontend/.env.local` 设置为 `NEXT_PUBLIC_USE_MOCKS=false`，前端连接真实 Supabase 服务；后端是否使用模型或 Mock 仍由其运行环境的 Mock 配置决定。

## 4. 健康卡片界面与移动端布局

- 编辑器提供九个分类 Tab：身体症状、睡眠作息、情绪、月经出血、体重食欲、运动、饮食饮品、用药、生活就医。
- 日期栏、分类栏和“今日健康卡片”提示条为共用固定头部；所有 Tab 的提示条位置一致。
- “返回对话”“保存记录”是固定底部操作栏；仅中间的分类内容区滚动。
- 每个分类支持增、删、改；日期可切换历史记录。保存使用 `save_health_record` RPC。
- 以移动端为优先：真实设备使用 `100vw × 100dvh` 和安全区；桌面预览保持 390×844 的手机壳比例；内部网格、原生日期控件、横向分类栏均做宽度约束，避免横向溢出。窄于 380px 的表单自动单列。

## 5. 验证记录

- `frontend/npm.cmd run build`：通过。
- `npx.cmd oxlint`：对更新的前端文件通过。
- `git diff --check`：通过（现有工作区的 CRLF/全局 ignore 警告不影响检查结果）。

## 6. 关键代码位置

- `frontend/app/page.tsx`：对话 UI、Markdown、语音、SSE 事件与更新提示。
- `frontend/components/health-record-editor.tsx`：健康卡片分类编辑、固定头尾布局与保存。
- `frontend/app/fidelity.css`、`frontend/app/globals.css`：移动端比例、健康卡片布局与样式。
- `frontend/lib/services.ts`：Mock/真实服务适配层。
- `supabase/functions/chat/index.ts`：对话编排、RAG、自动提取和自动写入。
- `supabase/functions/_shared/model.ts`：模型结构化提取、校验与修复。
- `supabase/functions/_shared/mock-ai.ts`：Mock 规则提取。

## 7. AI 帮我说界面更新

- “我的消息”入口卡片取消重复的文字、语音和开始按钮，只保留“试着说说”主入口，并明确“保留真实意思、支持文字或语音”。
- 表达页改为三步任务流：说出想法 → 整理表达 → 复制发送。
- 支持选择表达对象（伴侣、家人、朋友、同事或不指定），该上下文会传给改写服务。
- 提供“表达感受、提出请求、设定边界”快捷示例，降低首次使用门槛。
- 语音按钮已接入 `MediaRecorder` 与 `speech-asr`，转写后回填文本，由用户确认后再整理。
- 初始状态不再展示默认生成结果；生成后显示独立、可编辑的结果卡片。
- “发送 / 系统分享”为主操作，“复制”为次操作，“重新整理一版”为辅助操作。
- 增加“仅用于本次表达整理，不会写入健康卡片”的隐私提示，并按移动端独立滚动工作区适配。
